name: "Build"
on:
  workflow_dispatch:
  push:
    branches:
      - develop
      - feature/amnezia-premium-gateway

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: write

jobs:
  frontend:
    name: Build frontend
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd

      - name: Setup Node.js
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Build frontend
        run: ./scripts/build-frontend.sh

      - name: Upload frontend build
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: frontend-build
          path: frontend/build
          if-no-files-found: error

  version:
    name: Resolve IPK version
    runs-on: ubuntu-latest
    timeout-minutes: 5
    outputs:
      ipk_version: ${{ steps.resolve.outputs.ipk_version }}

    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd

      - name: Resolve revision for current patch
        id: resolve
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail

          VERSION="$(tr -d '[:space:]' < VERSION)"
          REV=1
          PREV=""

          mkdir -p /tmp/prev-release
          if gh release download latest --dir /tmp/prev-release --pattern VERSION 2>/dev/null \
            && [[ -f /tmp/prev-release/VERSION ]]; then
            PREV="$(tr -d '[:space:]' < /tmp/prev-release/VERSION)"
            esc="${VERSION//./\\.}"
            if [[ "$PREV" =~ ^${esc}\+r([0-9]+)$ ]]; then
              REV=$((BASH_REMATCH[1] + 1))
            fi
          fi

          IPK_VERSION="${VERSION}+r${REV}"
          echo "Resolved ${IPK_VERSION} (previous: ${PREV:-none})"
          echo "ipk_version=${IPK_VERSION}" >> "$GITHUB_OUTPUT"

  build:
    name: Build (${{ matrix.arch }})
    runs-on: ubuntu-latest
    needs:
      - frontend
      - version
    timeout-minutes: 120
    strategy:
      fail-fast: true
      matrix:
        arch:
          - mipsel-3.4
          - mips-3.4
          - aarch64-3.10

    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd

      - name: Download frontend build
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c
        with:
          name: frontend-build
          path: frontend/build

      - name: Setup go
        uses: actions/setup-go@4a3601121dd01d1626a1e23e37211e3254c1c06c
        with:
          go-version: 1.26.4

      - name: Install dependencies
        run: |
          set -euo pipefail

          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            git ca-certificates gnupg curl file xz-utils unzip zip python3 python3-requests procps \
            build-essential pkg-config

      # sing-box НЕ собирается здесь: наш sing-box (AWG3+mieru+xhttp) собирает CI
      # форка hoaxisr/amnezia-box и публикует релизом. embedded.go (закоммичен,
      # regen-embedded.sh) несёт URL/SHA256/Size его ассетов; роутер скачивает
      # бинарь в рантайме. IPK sing-box не содержит.

      - name: Build
        id: build
        run: |
          set -euo pipefail

          IPK_VERSION="${{ needs.version.outputs.ipk_version }}"
          # dist/ раньше создавали sing-box-шаги (удалены в #589)
          mkdir -p dist
          printf '%s\n' "$IPK_VERSION" > dist/VERSION

          SKIP_FRONTEND_BUILD=1 ./scripts/build-ipk.sh "$IPK_VERSION" "${{ matrix.arch }}"

      - name: Upload result
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: result-${{ matrix.arch }}
          path: |
            dist/awg-manager_*_${{ matrix.arch }}-kn.ipk
            dist/VERSION
          if-no-files-found: error

  test:
    name: Test
    runs-on: ubuntu-latest
    needs:
      - build

    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd

      - name: Setup Node.js
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Setup go
        uses: actions/setup-go@4a3601121dd01d1626a1e23e37211e3254c1c06c
        with:
          go-version: 1.26.4

      - name: Run go tests with backend coverage
        shell: bash
        run: |
          set -euo pipefail

          go test -count=1 \
            -covermode=atomic \
            -coverpkg=./internal/...,./cmd/... \
            -coverprofile=coverage.out \
            ./...

          go tool cover -func=coverage.out | tee coverage.txt
          go tool cover -html=coverage.out -o coverage.html

      - name: Run awgmproto module tests
        # awgmproto — вложенный модуль со своим go.mod: в `go test ./...` из
        # корня он НЕ попадает, поэтому у него отдельный шаг. Библиотеку тянут
        # три чужих репозитория, и её тесты обязаны идти в CI, а не только
        # локально. Гонки здесь профильные: слушатель пишет из нескольких
        # горутин.
        working-directory: awgmproto
        run: go test ./... -count=1 -race

      - name: Upload backend coverage
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: backend-coverage
          path: |
            coverage.out
            coverage.txt
            coverage.html
          if-no-files-found: error

      - name: Install frontend dependencies
        working-directory: frontend
        run: npm ci

      - name: Run frontend tests
        working-directory: frontend
        run: npx vitest run

  race:
    name: Race detector
    runs-on: ubuntu-latest
    # Отдельной джобой, а НЕ флагом к прогону с покрытием — три причины:
    # падение от гонки отличимо от падения теста по самой джобе; артефакт
    # покрытия остаётся дешёвым (-race даёт +54% времени, замер 2026-09-01:
    # 35 с → 54 с локально); и эти +54% ложатся на параллельную задачу, а не
    # на критический путь до pre-release.
    #
    # Что он реально ловит: детектор в Go строит векторные часы и сообщает о
    # гонке, если два конфликтующих доступа случились в одном прогоне без
    # связи happens-before. «Неудачное» переплетение для этого не нужно —
    # поэтому двух ядер раннера достаточно. Гарантии всё же нет: теневое
    # состояние ограничено (несколько слотов на слово), и редкую пару
    # доступов может вытеснить.
    # Видит он при этом ТОЛЬКО исполняемый тестами код: по непокрытым путям
    # (а их много) молчит по построению.
    #
    # Главный модуль до 2026-09-01 под -race в CI не гонялся вовсе — гонки
    # ловились случайно и локально (две снято в ветке fix/flaky-ci-tests).
    needs:
      - build

    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd

      - name: Setup go
        uses: actions/setup-go@4a3601121dd01d1626a1e23e37211e3254c1c06c
        with:
          go-version: 1.26.4

      - name: Run go tests with race detector
        # Без -coverpkg и без профиля: покрытие снимает джоба Test, а здесь
        # важен только сам детектор. -count=1 обязателен — кэш результатов
        # спрятал бы уже пройденные пакеты.
        run: go test ./... -count=1 -race

  pre-release:
    name: Pre-release
    if: github.ref == 'refs/heads/develop'
    runs-on: ubuntu-latest
    needs:
      - build
      - test
      - race
      - version

    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
        with:
          fetch-depth: 0

      - name: Download build results
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c
        with:
          pattern: result-*
          path: dist
          merge-multiple: true

      - name: Setup go
        uses: actions/setup-go@4a3601121dd01d1626a1e23e37211e3254c1c06c
        with:
          go-version: 1.26.4

      - name: Generate develop changelog
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          IPK_VERSION: ${{ needs.version.outputs.ipk_version }}
          EVENT_BEFORE: ${{ github.event.before }}
        shell: bash
        run: |
          set -euo pipefail

          go build -o /tmp/changelog-gen ./cmd/changelog-gen

          mkdir -p /tmp/prevcl
          gh release download latest --dir /tmp/prevcl \
            --pattern CHANGELOG.md --pattern RELEASED_SHA 2>/dev/null || true

          START=""
          if [[ -f /tmp/prevcl/RELEASED_SHA ]]; then
            START="$(tr -d '[:space:]' < /tmp/prevcl/RELEASED_SHA)"
          fi
          if [[ -z "$START" ]] || ! git cat-file -e "${START}^{commit}" 2>/dev/null; then
            START="$EVENT_BEFORE"
          fi
          if [[ -z "$START" ]] || [[ "$START" =~ ^0+$ ]] || ! git cat-file -e "${START}^{commit}" 2>/dev/null; then
            START="${GITHUB_SHA}~1"
          fi

          echo "changelog range: ${START}..${GITHUB_SHA}"
          git log --format='%s' "${START}..${GITHUB_SHA}" \
            | /tmp/changelog-gen --version "$IPK_VERSION" --date "$(date +%F)" > /tmp/delta.md

          PREV=/tmp/prevcl/CHANGELOG.md
          [[ -f "$PREV" ]] || PREV=/dev/null
          if [[ -s /tmp/delta.md ]]; then
            cat /tmp/delta.md "$PREV" > dist/CHANGELOG.md
          else
            cat "$PREV" > dist/CHANGELOG.md
          fi
          printf '%s\n' "$GITHUB_SHA" > dist/RELEASED_SHA
          echo "=== dist/CHANGELOG.md (head) ==="; head -20 dist/CHANGELOG.md || true

      - name: Publish pre-release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        shell: bash
        run: |
          set -euo pipefail

          shopt -s nullglob
          files=(./dist/awg-manager_* ./dist/VERSION ./dist/CHANGELOG.md ./dist/RELEASED_SHA)

          if (( ${#files[@]} == 0 )); then
            echo "No release files found in ./dist"
            exit 1
          fi

          gh release delete latest --yes --cleanup-tag || true

          PREV_TAG=$(gh release list --limit 10 --json tagName --jq '[.[] | select(.tagName != "latest")] | .[0].tagName' 2>/dev/null)

          EXTRA_FLAGS=()
          if [ -n "${PREV_TAG}" ]; then
            EXTRA_FLAGS+=("--notes-start-tag" "${PREV_TAG}")
          fi

          gh release create latest "${files[@]}" \
            --target "${GITHUB_SHA}" \
            --title "Development build" \
            --generate-notes \
            "${EXTRA_FLAGS[@]}" \
            --prerelease
